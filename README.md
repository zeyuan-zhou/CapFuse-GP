# CapFuse-GP

Official implementation of CapFuse-GP for genomic prediction in rice.

## Requirements

- Python 3
- PyTorch
- NumPy
- pandas
- scikit-learn

## Usage

The model is implemented in `CapFuse_GP.py`.

Run:

```bash
python CapFuse_GP.py --help
